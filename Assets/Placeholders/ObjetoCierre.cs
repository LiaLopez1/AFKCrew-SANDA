using UnityEngine;

public class ObjetoCierre : MonoBehaviour
{
    //[SerializeField] private float tiempoDeCierre = 1.5f; // Cuánto tarda en cerrarse

    //// Se activa cuando algo entra en el Trigger del objeto
    //private void OnTriggerEnter(Collider other)
    //{
    //    // Verificamos si lo que tocó el objeto es el Jugador
    //    if (other.CompareTag("Player"))
    //    {
    //        // Buscamos el gestor que creamos en el Paso 2 y activamos el cierre
    //        ControlVignette gestor = Object.FindFirstObjectByType<ControlVignette>();

    //        if (gestor != null)
    //        {
    //            gestor.IniciarCierre(tiempoDeCierre);
    //        }

    //        // Opcional: Destruye este objeto para que no se vuelva a tocar
    //        Destroy(gameObject);
    //    }
    //}
}
